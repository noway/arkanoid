#ifndef UTIL_HEADER_ONCE
#define UTIL_HEADER_ONCE

#include <Windows.h>
#include "Main.h"

void move_cursor_to(int row, int column);
void init_block_matrix(block blocks[BLOCKS_ROWS][BLOCKS_COLUMNS]);


#endif